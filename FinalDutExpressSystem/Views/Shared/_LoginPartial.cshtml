@using Microsoft.AspNet.Identity

@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
    @Html.AntiForgeryToken()

    <ul class="nav navbar-nav navbar-right">
        <li>

        <li class="dropdown">
            <a href="#" class="dropdown-toggle"
               data-toggle="dropdown">
                <i class="glyphicon glyphicon-user">
                </i> @*@Html.DisplayFor(Model => Model.StudentNo)*@ <b class="caret"></b>
            </a>

                    <ul class="dropdown-menu">
                        <li>
                            @Html.ActionLink("iEnabler", "Index", "itsModels", routeValues: null, htmlAttributes: new { title = "Admin Panel" })
                        </li>

                        <li>
                            @Html.ActionLink( "Courses", "Index", "CourseModels", routeValues: null, htmlAttributes: new { title = "Admin Panel" })
                        </li>
                        <li>
                            @Html.ActionLink("Library", "Library", "Library", routeValues: null, htmlAttributes: new { title = "Library" })
                        </li>
                        <li>
                          @Html.ActionLink("Blackboard", "Index", "Blackboard", routeValues: null, htmlAttributes: new { title = "Blackboard" })
                        </li>
                        <li class="divider"></li>
                        <li>

                            <a href="javascript:document.getElementById('logoutForm').submit()"><i class="glyphicon glyphicon-off"></i> Log Off</a>
                        </li>
                    </ul>
                </li>
        <li>
                @Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
            </li>
        <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
    </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
   
        
         <li>@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    
    
    </ul>
}
